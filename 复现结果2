上一个word文件中，我复现了table1，论文的第6页，论证了cca在不同数据集上面比其他模型得到了更好的结果。
这次，我要进行的是table2。

消融实验ablation：论文提出了一个方法,这个方法里有好几个组件(这里是 CCA、RFP、FTT 三个)。
        作者想证明"每个组件都有用",做法就是一个一个地往基础模型上加组件,看准确率怎么涨。
        （类似控制单一变量？）
第一个组件：CCA(Canonical Correlation Analysis,典型相关分析)        
   通过算协方差和相关性（不需要训练）来找到新的空间，让他们靠齐，尽可能重叠。（就是跟之前说的投射到一个新的子向量空间差不多）
           把这个特征（图像或文本）乘上一个特定的矩阵，得到和另一个（文本或图像），这个矩阵是一个微小的变化，所以计算量不大，不需要训练。
  论文中，加上cca之后，accuracy从 66.7 跳到 76.5。
  第二个组件：RFP(Random Fourier Projection,随机傅里叶投影)
      cca只能找线性的关系，而我们的真实函数往往是复杂的，不能通过简单地线性来进行准确描述，所以引入非线性的元素来让函数变成准确率更高的曲线。
      而随机傅里叶投影就是通过cos和sin这两个进行变换，实现了非线性的曲线。
      （实际公式中只用了cos，
      然而在实际操作中，其实随机傅里叶投影应用的很简单，只是在刚开始就加进去，进行随机的曲线变换，让函数有了描述曲线的能力。这就是他的全部作用。
          类似在GAN中，我们需要生成不同的图片，所以需要加noise，这个noise就增添了随机性，把函数的维度拉高了，有了描述曲线的能力。
        加上 RFP 后从 76.5 涨到 81.0,涨幅不如 CCA 大,但确实有效,
  第三个组件：FTT(First-Task Training,首任务微调)
        在持续学习开始之前，我们先用一个小小的学习率让clip模型在第一个任务上轻微微调几个epoch（轮次），在另一个仓库papers中我提到的第一步，
        就是热身一下。之后再进行冻结块。
        这次从   81.0 涨到 83.0。
